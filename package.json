{
  "name": "vicent-manila-portfolio",
  "private": true,
  "version": "5.5.0",
  "description": "Static portfolio QA tooling for Vicent Manila",
  "scripts": {
    "validate": "node scripts/validate-site.mjs",
    "test:responsive": "playwright test tests/responsive.spec.mjs tests/case-studies.spec.mjs --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "1.52.0"
  }
}
